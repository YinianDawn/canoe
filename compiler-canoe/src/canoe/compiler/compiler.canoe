package compiler

Compiler :opened {
    options :Options
    compile {
        
    }
    new(args string[]) -> Compiler {
        return Compiler {
            options: 
        }
    }
}
